class Solution {
    public static int[] plusOne(int[] digits) {
        // ArrayList<Integer> newDigits = new ArrayList<Integer>();
        // String strFromArr = "";
        // int intFromArr = 0;
        // for (int i = 0; i < digits.length; i++) {
        //     strFromArr = strFromArr + Integer.toString(digits[i]);
        // }
        // intFromArr = Integer.parseInt(strFromArr) + 1;

        // for (int k = 0; k <= digits.length; k++) {
        //     int rem = intFromArr % 10; // 123 % 10 = 3,  12 % 10 = 2, 1 % 10 = 1
        //     intFromArr = intFromArr / 10; // 123 / 10 = 12, 12 / 10 = 1, 1 / 10 = 0
        //     newDigits.add(rem);
        // }

        // if (newDigits.get(0) == 0) {
        //     newDigits.remove(0);
        // }

        // int[] ret = new int[newDigits.size()];
        // for (int i=0; i < ret.length; i++)
        // {
        //     ret[i] = newDigits.get(i).intValue();
        // }
        // return ret;
    }
}